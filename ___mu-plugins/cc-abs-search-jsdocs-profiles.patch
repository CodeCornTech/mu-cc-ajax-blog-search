--- a/cc-ajax-blog-search-search.js
+++ b/cc-ajax-blog-search-search.js
@@ -22,7 +22,7 @@
  * - gestione sidebar / overlay mobile;
  * - query WordPress lato server.
  *
- * @version 1.1.1-debug
+ * @version 1.2.0-debug
  */
 
 (function (w, $) {
@@ -51,7 +51,9 @@
      *     nonce,
      *     debug,
      *     context,
-     *     ...
+     *     profiles,
+     *     show_thumb,
+     *     ...
      * }
      */
     const CFG = w.CC_Ajax_Blog_Search || null;
@@ -68,6 +70,30 @@
      */
     const ABS = w.CC_ABS || null;
 
+    /**
+     * Profilo normalizzato associato a uno o più form di ricerca.
+     *
+     * Ogni profilo mantiene indipendenti:
+     * - selettori DOM;
+     * - scope logico;
+     * - post type ammessi;
+     * - etichetta mostrata nel dropdown;
+     * - abilitazione delle thumbnail.
+     *
+     * @typedef {Object} CCABSSearchProfile
+     * @property {string} id Identificatore univoco del profilo.
+     * @property {string} scope Scope logico inviato al server.
+     * @property {Array<string>} post_type Post type inviati al server.
+     * @property {Array<string>} selectors Selettori dei form associati.
+     * @property {string} label Etichetta leggibile del contesto.
+     * @property {boolean} show_thumb Abilitazione thumbnail.
+     */
+
     // ------------------------------------------------------------
     // Risoluzione debug
     // ------------------------------------------------------------
@@ -145,43 +171,145 @@
     // Risoluzione selettori
     // ------------------------------------------------------------
 
+    /**
+     * Normalizza una lista di stringhe proveniente dalla configurazione PHP.
+     *
+     * La normalizzazione:
+     * - scarta valori non stringa;
+     * - rimuove spazi iniziali e finali;
+     * - elimina stringhe vuote;
+     * - elimina duplicati mantenendo l'ordine originale.
+     *
+     * @param {*} value
+     * @return {Array<string>}
+     */
+    function normalizeStringList(value) {
+        if (!Array.isArray(value)) {
+            return [];
+        }
+
+        return [
+            ...new Set(
+                value
+                    .filter((item) => typeof item === 'string')
+                    .map((item) => item.trim())
+                    .filter(Boolean)
+            ),
+        ];
+    }
+
+    /**
+     * Profilo compatibile con la configurazione storica del plugin.
+     *
+     * Viene usato quando:
+     * - PHP non espone alcun profilo esplicito;
+     * - un form non corrisponde a nessun profilo configurato;
+     * - si mantiene una configurazione precedente all'introduzione
+     *   della ricerca multi-profilo.
+     *
+     * @type {CCABSSearchProfile}
+     */
     const legacyProfile = {
         id: 'legacy',
         scope: CFG.context?.scope || 'global',
-        post_type: Array.isArray(CFG.context?.post_type) ? CFG.context.post_type : [],
-        selectors: Array.isArray(CFG.context?.selectors) && CFG.context.selectors.length ? CFG.context.selectors.filter((item) => typeof item === 'string' && item.trim()) : ['.search-form'],
+        post_type: normalizeStringList(CFG.context?.post_type),
+        selectors: normalizeStringList(CFG.context?.selectors).length
+            ? normalizeStringList(CFG.context?.selectors)
+            : ['.search-form'],
         label: '',
         show_thumb: !!CFG.show_thumb,
     };
 
-    const rawProfiles = CFG.profiles && typeof CFG.profiles === 'object' ? CFG.profiles : {};
+    /**
+     * Profili grezzi generati dal filtro PHP:
+     *
+     *     cc_ajax_blog_search_profiles
+     *
+     * La configurazione attesa è un oggetto indicizzato per ID:
+     *
+     * {
+     *     bbm_press: {
+     *         selectors: [...],
+     *         scope: 'bbm_press',
+     *         post_type: ['post'],
+     *         label: 'Press',
+     *         show_thumb: true
+     *     },
+     *     bbm_header: {
+     *         selectors: [...],
+     *         scope: 'bbm_header',
+     *         post_type: ['page', 'post'],
+     *         label: 'sito',
+     *         show_thumb: false
+     *     }
+     * }
+     *
+     * @type {Object<string, Object>}
+     */
+    const rawProfiles =
+        CFG.profiles &&
+        typeof CFG.profiles === 'object' &&
+        !Array.isArray(CFG.profiles)
+            ? CFG.profiles
+            : {};
 
+    /**
+     * Profili espliciti normalizzati e realmente utilizzabili.
+     *
+     * I profili privi di selettori validi vengono esclusi perché
+     * non potrebbero essere associati ad alcun form.
+     *
+     * @type {Array<CCABSSearchProfile>}
+     */
     const configuredProfiles = Object.entries(rawProfiles)
         .map(([id, config]) => {
-            const profileSelectors = Array.isArray(config?.selectors) ? config.selectors.filter((item) => typeof item === 'string' && item.trim()) : [];
+            const profileSelectors = normalizeStringList(
+                config?.selectors
+            );
 
             return {
                 id,
-                scope: typeof config?.scope === 'string' && config.scope.trim() ? config.scope : 'global',
-                post_type: Array.isArray(config?.post_type) ? config.post_type.filter((item) => typeof item === 'string' && item.trim()) : [],
+                scope:
+                    typeof config?.scope === 'string' &&
+                    config.scope.trim()
+                        ? config.scope.trim()
+                        : 'global',
+                post_type: normalizeStringList(
+                    config?.post_type
+                ),
                 selectors: profileSelectors,
-                label: typeof config?.label === 'string' ? config.label : '',
-                show_thumb: typeof config?.show_thumb === 'boolean' ? config.show_thumb : !!CFG.show_thumb,
+                label:
+                    typeof config?.label === 'string'
+                        ? config.label.trim()
+                        : '',
+                show_thumb:
+                    typeof config?.show_thumb === 'boolean'
+                        ? config.show_thumb
+                        : !!CFG.show_thumb,
             };
         })
         .filter((profile) => profile.selectors.length);
 
-    const activeProfiles = configuredProfiles.length ? configuredProfiles : [legacyProfile];
+    /**
+     * Profili effettivamente attivi nella pagina corrente.
+     *
+     * La presenza di almeno un profilo esplicito disattiva il binding
+     * indiscriminato del profilo legacy, evitando che `.search-form`
+     * intercetti form non appartenenti alla configurazione corrente.
+     *
+     * @type {Array<CCABSSearchProfile>}
+     */
+    const activeProfiles = configuredProfiles.length
+        ? configuredProfiles
+        : [legacyProfile];
 
-    const selectors = [...new Set(activeProfiles.flatMap((profile) => profile.selectors))];
+    /**
+     * Lista piatta e deduplicata di tutti i selettori attivi.
+     *
+     * Viene utilizzata da:
+     * - scansione iniziale del DOM;
+     * - MutationObserver;
+     * - diagnostica.
+     *
+     * @type {Array<string>}
+     */
+    const selectors = [
+        ...new Set(
+            activeProfiles.flatMap(
+                (profile) => profile.selectors
+            )
+        ),
+    ];
 
+    /**
+     * Selettore aggregato jQuery.
+     *
+     * Una stringa vuota rappresenta l'assenza di form configurati.
+     *
+     * @type {string}
+     */
     const selector = selectors.join(',');
 
+    /**
+     * Determina il profilo associato a uno specifico form.
+     *
+     * I profili vengono controllati nell'ordine restituito da PHP.
+     * Il primo selettore corrispondente determina il profilo.
+     *
+     * Un selettore non valido viene ignorato e segnalato in debug,
+     * senza interrompere il bootstrap degli altri form.
+     *
+     * @param {HTMLFormElement|HTMLElement} form
+     * @return {CCABSSearchProfile}
+     */
     function resolveProfile(form) {
         const matchedProfile = activeProfiles.find((profile) => {
             return profile.selectors.some((profileSelector) => {
@@ -217,6 +345,8 @@
         selector,
         initialMatches: selector ? $(selector).length : 0,
         context: CFG.context || null,
+        configuredProfiles,
+        activeProfiles,
     });
 
     // ------------------------------------------------------------
@@ -260,17 +390,22 @@
      * Costruisce l'URL della ricerca WordPress completa.
      *
-     * Nel contesto globale viene inviato soltanto:
-     *     ?s=termine
-     *
-     * Nei contesti scoped vengono aggiunti:
+     * L'URL conserva il profilo che ha originato la ricerca,
+     * permettendo alla SERP WordPress di applicare lo stesso perimetro
+     * della richiesta AJAX.
+     *
+     * Parametri prodotti:
+     * - s;
+     * - cc_abs_profile, salvo profilo legacy;
      * - post_type[];
      * - scope.
      *
      * @param {string} term
+     * @param {CCABSSearchProfile} profile
      * @return {string}
      */
     function buildSearchUrl(term, profile) {
         const params = new URLSearchParams();
 
         params.set('s', term);
 
@@ -295,13 +430,16 @@
      * Costruisce il footer con collegamento alla pagina risultati completa.
      *
      * @param {string} term
+     * @param {CCABSSearchProfile} profile
      * @return {string}
      */
-    function buildFooter(term) {
+    function buildFooter(term, profile) {
         return `
             <div class="cc-ajax-search-footer">
-                <a class="cc-ajax-search-all" href="${buildSearchUrl(term)}">
+                <a
+                    class="cc-ajax-search-all"
+                    href="${buildSearchUrl(term, profile)}"
+                >
                     Mostra tutti i risultati →
                 </a>
             </div>
@@ -316,6 +454,7 @@
      * così eventuale markup digitato dall'utente non viene interpretato.
      *
      * @param {string} term
+     * @param {CCABSSearchProfile} profile
      * @return {string}
      */
     function buildHeader(term, profile) {
@@ -326,16 +465,26 @@
             global: 'globale',
         };
 
-        const scopeLabel = profile.label || labels[profile.scope] || profile.scope;
+        /**
+         * Ordine di risoluzione:
+         * 1. label esplicita del profilo;
+         * 2. label standard dello scope;
+         * 3. valore grezzo dello scope;
+         * 4. fallback generico.
+         */
+        const scopeLabel =
+            profile.label ||
+            labels[profile.scope] ||
+            profile.scope ||
+            'ricerca';
 
-        const safeTerm = $('<div>').text(term).html();
+        const safeTerm = $('<div>')
+            .text(term)
+            .html();
 
         return `
-        <div class="cc-ajax-search-heading">
-            Risultati per "<strong>${safeTerm}</strong>" — ${scopeLabel}
-        </div>
-    `;
+            <div class="cc-ajax-search-heading">
+                Risultati per "<strong>${safeTerm}</strong>" — ${scopeLabel}
+            </div>
+        `;
     }
 
     /**
@@ -367,6 +516,7 @@
      * @param {JQuery} $box
      * @param {Array<object>} items
      * @param {string} term
+     * @param {CCABSSearchProfile} profile
      * @return {void}
      */
     function renderResults($box, items, term, profile) {
@@ -374,6 +524,11 @@
             term,
             count: items?.length || 0,
             items,
+            profile: {
+                id: profile.id,
+                scope: profile.scope,
+                post_type: profile.post_type,
+                show_thumb: profile.show_thumb,
+            },
         });
 
         // Nessun risultato restituito dalla query.
@@ -475,6 +630,7 @@
         if ($form.data('ccAjaxSearchInited')) {
             return false;
         }
+
         const profile = resolveProfile(form);
 
         $form.attr('data-cc-abs-profile', profile.id);
+
         /**
          * WordPress usa normalmente:
          *     input[name="s"]
@@ -528,6 +684,12 @@
          */
         let xhr = null;
 
+        /**
+         * Il profilo viene risolto una sola volta durante il binding.
+         *
+         * Ogni form conserva quindi il proprio contesto anche quando
+         * nella stessa pagina sono presenti altri form ABS.
+         */
+
         /**
          * Esegue realmente la ricerca.
          *
@@ -667,6 +829,12 @@
             reason,
             form,
             input: $input.get(0),
+            profile: {
+                id: profile.id,
+                scope: profile.scope,
+                post_type: profile.post_type,
+                selectors: profile.selectors,
+                show_thumb: profile.show_thumb,
+            },
         });
 
         return true;
```

## Creazione della patch

Il comando individua il file senza assumere il percorso interno preciso.

```bash
#!/usr/bin/env bash

TARGET="$(
    find wp-content/mu-plugins \
        -type f \
        -name 'cc-ajax-blog-search-search.js' \
        -print \
        -quit
)"

PATCH_FILE="/tmp/cc-abs-search-jsdocs-profiles.patch"

if [ -z "$TARGET" ]; then
    printf '%s\n' \
        '[BLOCCATO] cc-ajax-blog-search-search.js non trovato.'
else
    printf 'TARGET=%s\n' "$TARGET"

    cat > "$PATCH_FILE" <<'PATCH'
--- a/cc-ajax-blog-search-search.js
+++ b/cc-ajax-blog-search-search.js
@@ -22,7 +22,7 @@
  * - gestione sidebar / overlay mobile;
  * - query WordPress lato server.
  *
- * @version 1.1.1-debug
+ * @version 1.2.0-debug
  */
 
 (function (w, $) {
@@ -51,7 +51,9 @@
      *     nonce,
      *     debug,
      *     context,
-     *     ...
+     *     profiles,
+     *     show_thumb,
+     *     ...
      * }
      */
     const CFG = w.CC_Ajax_Blog_Search || null;
@@ -68,6 +70,30 @@
      */
     const ABS = w.CC_ABS || null;
 
+    /**
+     * Profilo normalizzato associato a uno o più form di ricerca.
+     *
+     * Ogni profilo mantiene indipendenti:
+     * - selettori DOM;
+     * - scope logico;
+     * - post type ammessi;
+     * - etichetta mostrata nel dropdown;
+     * - abilitazione delle thumbnail.
+     *
+     * @typedef {Object} CCABSSearchProfile
+     * @property {string} id Identificatore univoco del profilo.
+     * @property {string} scope Scope logico inviato al server.
+     * @property {Array<string>} post_type Post type inviati al server.
+     * @property {Array<string>} selectors Selettori dei form associati.
+     * @property {string} label Etichetta leggibile del contesto.
+     * @property {boolean} show_thumb Abilitazione thumbnail.
+     */
+
     // ------------------------------------------------------------
     // Risoluzione debug
     // ------------------------------------------------------------
@@ -145,43 +171,145 @@
     // Risoluzione selettori
     // ------------------------------------------------------------
 
+    /**
+     * Normalizza una lista di stringhe proveniente dalla configurazione PHP.
+     *
+     * La normalizzazione:
+     * - scarta valori non stringa;
+     * - rimuove spazi iniziali e finali;
+     * - elimina stringhe vuote;
+     * - elimina duplicati mantenendo l'ordine originale.
+     *
+     * @param {*} value
+     * @return {Array<string>}
+     */
+    function normalizeStringList(value) {
+        if (!Array.isArray(value)) {
+            return [];
+        }
+
+        return [
+            ...new Set(
+                value
+                    .filter((item) => typeof item === 'string')
+                    .map((item) => item.trim())
+                    .filter(Boolean)
+            ),
+        ];
+    }
+
+    /**
+     * Profilo compatibile con la configurazione storica del plugin.
+     *
+     * Viene usato quando:
+     * - PHP non espone alcun profilo esplicito;
+     * - un form non corrisponde a nessun profilo configurato;
+     * - si mantiene una configurazione precedente all'introduzione
+     *   della ricerca multi-profilo.
+     *
+     * @type {CCABSSearchProfile}
+     */
     const legacyProfile = {
         id: 'legacy',
         scope: CFG.context?.scope || 'global',
-        post_type: Array.isArray(CFG.context?.post_type) ? CFG.context.post_type : [],
-        selectors: Array.isArray(CFG.context?.selectors) && CFG.context.selectors.length ? CFG.context.selectors.filter((item) => typeof item === 'string' && item.trim()) : ['.search-form'],
+        post_type: normalizeStringList(CFG.context?.post_type),
+        selectors: normalizeStringList(CFG.context?.selectors).length
+            ? normalizeStringList(CFG.context?.selectors)
+            : ['.search-form'],
         label: '',
         show_thumb: !!CFG.show_thumb,
     };
 
-    const rawProfiles = CFG.profiles && typeof CFG.profiles === 'object' ? CFG.profiles : {};
+    /**
+     * Profili grezzi generati dal filtro PHP:
+     *
+     *     cc_ajax_blog_search_profiles
+     *
+     * @type {Object<string, Object>}
+     */
+    const rawProfiles =
+        CFG.profiles &&
+        typeof CFG.profiles === 'object' &&
+        !Array.isArray(CFG.profiles)
+            ? CFG.profiles
+            : {};
 
+    /**
+     * Profili espliciti normalizzati e realmente utilizzabili.
+     *
+     * @type {Array<CCABSSearchProfile>}
+     */
     const configuredProfiles = Object.entries(rawProfiles)
         .map(([id, config]) => {
-            const profileSelectors = Array.isArray(config?.selectors) ? config.selectors.filter((item) => typeof item === 'string' && item.trim()) : [];
+            const profileSelectors = normalizeStringList(
+                config?.selectors
+            );
 
             return {
                 id,
-                scope: typeof config?.scope === 'string' && config.scope.trim() ? config.scope : 'global',
-                post_type: Array.isArray(config?.post_type) ? config.post_type.filter((item) => typeof item === 'string' && item.trim()) : [],
+                scope:
+                    typeof config?.scope === 'string' &&
+                    config.scope.trim()
+                        ? config.scope.trim()
+                        : 'global',
+                post_type: normalizeStringList(
+                    config?.post_type
+                ),
                 selectors: profileSelectors,
-                label: typeof config?.label === 'string' ? config.label : '',
-                show_thumb: typeof config?.show_thumb === 'boolean' ? config.show_thumb : !!CFG.show_thumb,
+                label:
+                    typeof config?.label === 'string'
+                        ? config.label.trim()
+                        : '',
+                show_thumb:
+                    typeof config?.show_thumb === 'boolean'
+                        ? config.show_thumb
+                        : !!CFG.show_thumb,
             };
         })
         .filter((profile) => profile.selectors.length);
 
-    const activeProfiles = configuredProfiles.length ? configuredProfiles : [legacyProfile];
+    /**
+     * Profili effettivamente attivi nella pagina corrente.
+     *
+     * @type {Array<CCABSSearchProfile>}
+     */
+    const activeProfiles = configuredProfiles.length
+        ? configuredProfiles
+        : [legacyProfile];
 
-    const selectors = [...new Set(activeProfiles.flatMap((profile) => profile.selectors))];
+    /**
+     * Lista piatta e deduplicata di tutti i selettori attivi.
+     *
+     * @type {Array<string>}
+     */
+    const selectors = [
+        ...new Set(
+            activeProfiles.flatMap(
+                (profile) => profile.selectors
+            )
+        ),
+    ];
 
+    /**
+     * Selettore aggregato jQuery.
+     *
+     * @type {string}
+     */
     const selector = selectors.join(',');
 
+    /**
+     * Determina il profilo associato a uno specifico form.
+     *
+     * @param {HTMLFormElement|HTMLElement} form
+     * @return {CCABSSearchProfile}
+     */
     function resolveProfile(form) {
         const matchedProfile = activeProfiles.find((profile) => {
             return profile.selectors.some((profileSelector) => {
@@ -217,6 +345,8 @@
         selector,
         initialMatches: selector ? $(selector).length : 0,
         context: CFG.context || null,
+        configuredProfiles,
+        activeProfiles,
     });
 
     // ------------------------------------------------------------
@@ -260,17 +390,22 @@
      * Costruisce l'URL della ricerca WordPress completa.
      *
-     * Nel contesto globale viene inviato soltanto:
-     *     ?s=termine
-     *
-     * Nei contesti scoped vengono aggiunti:
+     * L'URL conserva il profilo che ha originato la ricerca,
+     * permettendo alla SERP WordPress di applicare lo stesso perimetro
+     * della richiesta AJAX.
+     *
+     * Parametri prodotti:
+     * - s;
+     * - cc_abs_profile, salvo profilo legacy;
      * - post_type[];
      * - scope.
      *
      * @param {string} term
+     * @param {CCABSSearchProfile} profile
      * @return {string}
      */
     function buildSearchUrl(term, profile) {
@@ -295,13 +430,16 @@
      * Costruisce il footer con collegamento alla pagina risultati completa.
      *
      * @param {string} term
+     * @param {CCABSSearchProfile} profile
      * @return {string}
      */
-    function buildFooter(term) {
+    function buildFooter(term, profile) {
         return `
             <div class="cc-ajax-search-footer">
-                <a class="cc-ajax-search-all" href="${buildSearchUrl(term)}">
+                <a
+                    class="cc-ajax-search-all"
+                    href="${buildSearchUrl(term, profile)}"
+                >
                     Mostra tutti i risultati →
                 </a>
             </div>
@@ -316,6 +454,7 @@
      * così eventuale markup digitato dall'utente non viene interpretato.
      *
      * @param {string} term
+     * @param {CCABSSearchProfile} profile
      * @return {string}
      */
     function buildHeader(term, profile) {
@@ -326,16 +465,23 @@
             global: 'globale',
         };
 
-        const scopeLabel = profile.label || labels[profile.scope] || profile.scope;
+        const scopeLabel =
+            profile.label ||
+            labels[profile.scope] ||
+            profile.scope ||
+            'ricerca';
 
-        const safeTerm = $('<div>').text(term).html();
+        const safeTerm = $('<div>')
+            .text(term)
+            .html();
 
         return `
-        <div class="cc-ajax-search-heading">
-            Risultati per "<strong>${safeTerm}</strong>" — ${scopeLabel}
-        </div>
-    `;
+            <div class="cc-ajax-search-heading">
+                Risultati per "<strong>${safeTerm}</strong>" — ${scopeLabel}
+            </div>
+        `;
     }
 
     /**
@@ -367,6 +513,7 @@
      * @param {JQuery} $box
      * @param {Array<object>} items
      * @param {string} term
+     * @param {CCABSSearchProfile} profile
      * @return {void}
      */
     function renderResults($box, items, term, profile) {
@@ -374,6 +521,11 @@
             term,
             count: items?.length || 0,
             items,
+            profile: {
+                id: profile.id,
+                scope: profile.scope,
+                post_type: profile.post_type,
+                show_thumb: profile.show_thumb,
+            },
         });
 
         // Nessun risultato restituito dalla query.
@@ -475,8 +627,10 @@
         if ($form.data('ccAjaxSearchInited')) {
             return false;
         }
+
         const profile = resolveProfile(form);
 
         $form.attr('data-cc-abs-profile', profile.id);
+
         /**
          * WordPress usa normalmente:
          *     input[name="s"]
@@ -667,6 +821,12 @@
             reason,
             form,
             input: $input.get(0),
+            profile: {
+                id: profile.id,
+                scope: profile.scope,
+                post_type: profile.post_type,
+                selectors: profile.selectors,
+                show_thumb: profile.show_thumb,
+            },
         });
 
         return true;
PATCH

    printf 'PATCH=%s\n' "$PATCH_FILE"
fi